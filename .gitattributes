# shell 与容器内脚本强制 LF，避免 Windows CRLF 导致容器里 /bin/sh 解释失败
*.sh            text eol=lf
Dockerfile      text eol=lf
*.dockerignore  text eol=lf
docker-compose.yml text eol=lf
nginx.conf      text eol=lf

# 其余由 Git 按平台自动处理
*               text=auto
